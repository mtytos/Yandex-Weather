import React from "react";
import style from "./Header.module.sass";

const Header = () => {
    return (
        <header className={style.header}>
            <p>Weather App created Mtytos</p>
        </header>
    )
};

export default Header;